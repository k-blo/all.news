# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]

# Cloudflare Wrangler
cron-worker/.wrangler/

# Crawl outputs — now stored in R2, not git (run `git rm -r --cached` at cutover).
# Kept out of the repo so the hourly crawl no longer bloats history.
/crawled.json
/index.html
/sitemap.xml
/archive/
/data/
# CI crawl artifacts
partial-*.json
partials/
